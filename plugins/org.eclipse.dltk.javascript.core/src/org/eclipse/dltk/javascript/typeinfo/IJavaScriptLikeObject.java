package org.eclipse.dltk.javascript.typeinfo;

/**
 * Just a marker interface for java objects that are javascript like objects (so
 * assignable to javascript Object) But they are also Java objects with
 * overloads and so on so they can't be marked as such.
 * 
 * @author jcompagner
 *
 */
public interface IJavaScriptLikeObject {

}
